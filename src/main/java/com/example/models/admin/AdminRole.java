package com.example.models.admin;

public enum AdminRole {
    /** Uses the admin panel. */
    ADMIN,
    /** The account all door staff share. It can only check tickets in at the entrance. */
    DOOR_STAFF
}
